# Story 1.1: Implement Magic Link Authentication

**Status**: Draft
**Story ID**: STORY-1.1
**Epic**: EPIC-1.0 (Authentication & Security Foundation)
**Created**: 2026-03-06
**Priority**: P0 - Critical
**Complexity**: Medium (5 points)
**Business Value**: Critical (CIPA compliance, user access)

---

## Title
Implement Magic Link Authentication

---

## Description

This story implements the core Magic Link authentication system that allows users to log in securely without passwords. Users receive a unique, time-limited link via email that authenticates them when clicked.

### Problem Statement
Users need a secure, passwordless way to access Safety Vote. Magic Link authentication provides this by:
1. Eliminating weak password vulnerabilities
2. Reducing support burden for password resets
3. Providing a frictionless user experience
4. Supporting LGPD/GDPR compliance (no password storage)

### User Need
As a **user**, I want to **log in securely without remembering a password**, so that **I can access Safety Vote quickly and safely**.

### Solution Overview

The Magic Link flow works as follows:

```
1. User visits login page and enters email
2. Frontend validates email format
3. Frontend sends POST /api/auth/send-magic-link with email
4. Backend validates email:
   - Check rate limiting (max 5 attempts/10 min per IP)
   - Check if user exists or can be registered
   - Generate cryptographically secure token
   - Save token to database with 24h expiration
5. Backend sends email with magic link
6. User receives email and clicks link containing token
7. Frontend detects token in URL (?token=...)
8. Frontend sends POST /api/auth/verify-magic-link with token
9. Backend validates token:
   - Check token exists and hasn't expired
   - Check token hasn't been used already
   - Mark token as used
   - Create JWT session token
   - Create audit log entry
10. Backend returns JWT token to frontend
11. Frontend stores JWT in localStorage/sessionStorage
12. Frontend redirects to dashboard
```

### Key Features
- **Passwordless Security**: No passwords to steal, guess, or reset
- **Time-Limited Tokens**: Links expire after 24 hours
- **Single-Use Tokens**: Each link can only be used once
- **Rate Limiting**: Max 5 requests per 10 minutes per IP address
- **Email Validation**: Sends unique link via email
- **Session Creation**: Automatic JWT session after link validation
- **Audit Trail**: All attempts logged for security monitoring
- **Mobile Friendly**: Works seamlessly on all devices

---

## Acceptance Criteria

### AC1: User can request Magic Link via email
**Given** a user visits the login page
**When** they enter a valid email address and click "Send Magic Link"
**Then**:
- [ ] System validates email format (RFC 5322 compliant)
- [ ] System checks rate limiting (max 5 attempts per 10 minutes per IP)
- [ ] If rate limit exceeded, display helpful error message with retry time
- [ ] If valid, display success message: "Check your email for a link to sign in"
- [ ] Email is sent within 2 seconds
- [ ] Audit log records "auth.magic_link_sent" action
- [ ] Error handling for email service failures

### AC2: Magic Link is sent via email with 24h expiration
**Given** a user requests a magic link
**When** the link is generated and sent
**Then**:
- [ ] Token is cryptographically secure (256-bit minimum)
- [ ] Token is unique and non-guessable
- [ ] Link in email contains token as query parameter: `?token=TOKEN`
- [ ] Email contains:
  - User-friendly message explaining the link
  - Clickable magic link with proper URL
  - Statement that link expires in 24 hours
  - Text instruction if link doesn't work
  - Security notice about not sharing link
- [ ] Token stored in database with expiration timestamp
- [ ] Tokens can only be used for 24 hours from generation
- [ ] After 24 hours, token is invalid and cannot be used

### AC3: Link validates user and creates session
**Given** a user clicks the magic link in their email
**When** the link is processed by the application
**Then**:
- [ ] Frontend extracts token from URL parameter
- [ ] Frontend sends token to `/api/auth/verify-magic-link`
- [ ] Backend validates token exists in database
- [ ] Backend validates token hasn't expired
- [ ] Backend validates token hasn't been used previously
- [ ] Backend marks token as used (prevents replay)
- [ ] Backend creates new user if doesn't exist (registration flow)
- [ ] Backend creates JWT session token
- [ ] JWT contains user_id, company_id, role, exp, iat
- [ ] Backend returns JWT and user info to frontend
- [ ] Frontend stores JWT in localStorage
- [ ] Frontend configures Authorization header: `Bearer {JWT}`
- [ ] Frontend redirects to appropriate dashboard (admin/rh/eleitor)
- [ ] Audit log records "auth.login" action with success status

### AC4: Rate limiting enforces max 5 attempts per 10 minutes per IP
**Given** a user/attacker attempts to request magic links
**When** they exceed 5 requests in a 10-minute window
**Then**:
- [ ] System tracks requests by IP address
- [ ] First 5 requests in 10 min window: allowed
- [ ] 6th request in same window: blocked
- [ ] Blocked request returns HTTP 429 (Too Many Requests)
- [ ] Response includes `Retry-After` header
- [ ] Response includes helpful error message
- [ ] Error message shows when user can retry
- [ ] Rate limit resets after 10 minutes without new requests
- [ ] Audit log records "auth.rate_limit_exceeded" action
- [ ] Rate limiting works per IP (not per email)
- [ ] Different IPs get independent rate limit buckets

### AC5: Token validation prevents security vulnerabilities
**Given** an attacker attempts various attacks
**When** they try to:
- [ ] Use expired token → Rejected with "Link expired"
- [ ] Use already-used token → Rejected with "Link already used"
- [ ] Modify token → Rejected with "Invalid token"
- [ ] Use invalid token → Rejected with "Invalid token"
- [ ] Reuse same link → Second attempt fails
- [ ] Use someone else's token → Works only for that user
**Then**: All attempts logged in audit_logs with failure status

### AC6: Error handling is user-friendly
**Given** various failure scenarios
**When** they occur
**Then**:
- [ ] Network error: "Network error, please try again"
- [ ] Email service down: "Unable to send email, please try later"
- [ ] Invalid email: "Please enter a valid email address"
- [ ] Rate limited: "Too many attempts. Please wait {minutes} before trying again"
- [ ] Link expired: "This link has expired. Please request a new one"
- [ ] Link already used: "This link has already been used"
- [ ] Server error: "Something went wrong. Please try again"
- [ ] Error messages don't leak security info
- [ ] All errors logged for debugging

---

## Scope

### In Scope
- Frontend login page with email input
- Magic link request endpoint (`POST /api/auth/send-magic-link`)
- Magic link verification endpoint (`POST /api/auth/verify-magic-link`)
- Magic token generation and storage
- Email template and delivery
- Rate limiting implementation
- JWT session creation
- Audit logging for auth events
- Error handling and user messaging
- Security validation (input sanitization, token uniqueness)
- Support for both login and registration flows

### Out of Scope
- CPF validation (can be added in future story)
- Multi-factor authentication (separate epic)
- Biometric authentication (future feature)
- SAML/OAuth integration (future feature)
- Password-based fallback (intentionally omitted)
- Phone/SMS magic links (email only for now)
- Passwordless FIDO2/WebAuthn (future feature)

---

## Dependencies

### Prerequisite Stories
- None (foundation story)

### Required Infrastructure
- [ ] PostgreSQL database with tables: users, magic_tokens, sessions, audit_logs
- [ ] Supabase Edge Functions or API layer ready
- [ ] Email service configured (nodemailer or Supabase emails)
- [ ] Frontend React application with TypeScript

### Third-Party Services
- [ ] Email service (configured and tested)
- [ ] Database (PostgreSQL via Supabase)

### Related Stories
- Story 1.2: Row Level Security (will secure the auth data)
- Story 1.3: Session Management (will handle token refresh)
- Story 1.4: Audit Logging (will store auth events)

---

## Risks

| Risk | Probability | Impact | Mitigation |
|------|-------------|--------|-----------|
| **Email delivery failures** | Medium | High | Implement retry logic, monitoring, fallback UI messages |
| **Token collision/predictability** | Low | Critical | Use crypto.randomBytes with 256-bit entropy, security review |
| **Race condition in token validation** | Medium | High | Use database transactions, unique constraints |
| **Rate limiting bypass** | Low | High | Implement both IP-based and per-email limits, monitor abuse |
| **Token leakage in logs** | Medium | Medium | Never log full tokens, only hash/prefix |
| **Email header injection** | Low | Medium | Use nodemailer safely, validate email addresses |
| **Session fixation** | Low | Medium | Generate new JWT on each login, rotate tokens |

---

## Criteria of Done

### Code
- [ ] All acceptance criteria implemented and passing
- [ ] Frontend components created (LoginForm, MagicLinkInput, etc.)
- [ ] Backend endpoints created and functional
- [ ] Email templates created and tested
- [ ] Database migrations for magic_tokens table
- [ ] Rate limiting middleware implemented
- [ ] Error handling comprehensive

### Quality
- [ ] Unit tests: ≥ 90% coverage for auth module
- [ ] Integration tests for full auth flow
- [ ] Security tests for token validation
- [ ] Rate limiting tested under load
- [ ] Email delivery tested
- [ ] Mobile responsiveness verified
- [ ] All tests passing locally

### Testing
- [ ] Manual testing on Chrome, Firefox, Safari, Edge
- [ ] Mobile testing (iOS and Android)
- [ ] Test invalid email formats
- [ ] Test rate limiting boundaries (4th, 5th, 6th request)
- [ ] Test token expiration
- [ ] Test token replay attacks
- [ ] Test concurrent requests
- [ ] Accessibility testing (WCAG AA)

### Security
- [ ] Security code review completed
- [ ] No secrets in code (API keys, emails)
- [ ] No sensitive data in logs
- [ ] HTTPS enforced in all API calls
- [ ] CSRF protection in place
- [ ] Input validation on frontend and backend
- [ ] Output encoding for email content

### Documentation
- [ ] Inline code comments for complex logic
- [ ] Function documentation (JSDoc)
- [ ] API documentation updated
- [ ] Security architecture documented
- [ ] Troubleshooting guide for common issues
- [ ] SETUP.md updated with auth requirements

### Deployment
- [ ] Code merged to main branch
- [ ] CI/CD pipeline passes
- [ ] Linting passes (ESLint)
- [ ] Type checking passes (TypeScript)
- [ ] Staging deployment successful
- [ ] No breaking changes
- [ ] Environment variables documented

---

## Technical Details

### Database Schema

#### magic_tokens table
```sql
CREATE TABLE magic_tokens (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  email VARCHAR(255) NOT NULL,
  token VARCHAR(256) UNIQUE NOT NULL,
  token_hash VARCHAR(64) UNIQUE NOT NULL, -- SHA-256 for lookup
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  company_id UUID NOT NULL REFERENCES companies(id),
  is_used BOOLEAN DEFAULT FALSE,
  used_at TIMESTAMP WITH TIME ZONE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  expires_at TIMESTAMP WITH TIME ZONE NOT NULL, -- NOW() + 24h
  ip_address INET,
  user_agent TEXT,
  CONSTRAINT token_not_reused CHECK (
    (is_used = FALSE) OR (is_used = TRUE AND used_at IS NOT NULL)
  )
);

CREATE INDEX idx_magic_tokens_token_hash ON magic_tokens(token_hash);
CREATE INDEX idx_magic_tokens_email ON magic_tokens(email);
CREATE INDEX idx_magic_tokens_expires_at ON magic_tokens(expires_at);
CREATE INDEX idx_magic_tokens_user_id ON magic_tokens(user_id);
```

#### Rate Limiting Storage (Redis or in-memory)
```
Key: rate_limit:{ip_address}:{email}
Value: { count: number, reset_at: timestamp }
```

### API Endpoints

#### POST /api/auth/send-magic-link
```json
Request:
{
  "email": "user@example.com"
}

Response (200):
{
  "success": true,
  "message": "Check your email for a sign-in link"
}

Response (429):
{
  "error": "too_many_requests",
  "message": "Too many attempts. Try again in 7 minutes",
  "retry_after_seconds": 420
}

Response (400):
{
  "error": "invalid_email",
  "message": "Please enter a valid email address"
}
```

#### POST /api/auth/verify-magic-link
```json
Request:
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}

Response (200):
{
  "success": true,
  "token": "eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": "uuid",
    "email": "user@example.com",
    "name": "User Name",
    "role": "eleitor",
    "company_id": "uuid"
  }
}

Response (400):
{
  "error": "invalid_token",
  "message": "This link is not valid or has expired"
}
```

### JWT Payload
```json
{
  "sub": "user_id_uuid",
  "email": "user@example.com",
  "company_id": "company_uuid",
  "role": "eleitor|rh|admin",
  "iat": 1709721600,
  "exp": 1709808000,
  "iss": "safety-vote",
  "aud": "safety-vote"
}
```

### Email Template

Subject: `Your sign-in link for Safety Vote`

```
Hi {user_name or "there"},

Click the button below to sign in to Safety Vote. This link will expire in 24 hours.

[Button: Sign In to Safety Vote]
https://safety-vote.app/auth/verify?token={token}

Or copy and paste this link in your browser:
https://safety-vote.app/auth/verify?token={token}

---

Security reminder:
- Never share this link with anyone
- This link is unique to your account
- If you didn't request this, just ignore this email

Questions? Contact us at support@safety-vote.com

---

Safety Vote Team
Built for secure CIPA elections
```

### Frontend Components

#### LoginForm.tsx
- Email input field with validation
- Loading state during submission
- Error messages
- Resend link option after initial request
- Mobile responsive
- Accessible (ARIA labels, keyboard navigation)

#### MagicLinkVerification.tsx
- Extract token from URL
- Automatic token verification
- Loading spinner
- Error handling with retry option
- Redirect on success
- Timeout handling

---

## Implementation Notes

### Security Best Practices
1. **Token Generation**: Use `crypto.randomBytes(32)` for 256-bit entropy
2. **Token Storage**: Never store plain token in database, only hash (SHA-256)
3. **Token Lookup**: Use hashed token for database queries
4. **Rate Limiting**: Implement with Redis or in-memory cache
5. **Email Security**: Don't log email addresses with tokens
6. **HTTPS Only**: All auth endpoints require HTTPS
7. **CSRF Protection**: Include CSRF token in form submission
8. **Input Validation**: Validate email on frontend and backend
9. **Error Messages**: Don't reveal user existence in error messages
10. **Audit Logging**: Log all attempts (success and failure)

### Performance Considerations
- Email sending should be asynchronous (queue-based)
- Database queries should use indexes on token_hash and email
- Rate limiting should use fast cache (Redis preferred)
- Token verification should complete in <100ms
- Consider caching user info after successful login

### Testing Strategy

#### Unit Tests
```typescript
// Token generation
test('generates cryptographically secure tokens');
test('tokens are unique');
test('tokens have sufficient entropy');

// Token validation
test('validates unexpired tokens');
test('rejects expired tokens');
test('rejects already-used tokens');
test('rejects tampered tokens');

// Rate limiting
test('allows first 5 requests');
test('blocks 6th request');
test('resets after 10 minutes');

// Email validation
test('accepts valid email formats');
test('rejects invalid email formats');
test('rejects email injection attempts');
```

#### Integration Tests
```typescript
// Full flow
test('user can login with magic link');
test('login creates JWT token');
test('JWT token is valid for API requests');

// Edge cases
test('concurrent requests from same IP');
test('token expires at exactly 24 hours');
test('email service failure handling');
```

### Frontend Implementation Notes
- Use form validation library (React Hook Form + Zod recommended)
- Store JWT in secure location (httpOnly cookie preferred)
- Implement token refresh mechanism
- Handle token expiration gracefully
- Provide clear feedback at each step
- Mobile-first responsive design

### Backend Implementation Notes
- Use prepared statements (Supabase does this by default)
- Implement input sanitization
- Use transactions for atomic operations
- Implement proper error handling
- Log security events
- Monitor for abuse patterns
- Set up alerting for unusual activity

---

## File List

### To Be Created
- [ ] `/src/lib/auth/token-generator.ts` - Token generation logic
- [ ] `/src/lib/auth/token-validator.ts` - Token validation logic
- [ ] `/src/lib/auth/rate-limiter.ts` - Rate limiting implementation
- [ ] `/src/lib/auth/email-service.ts` - Email sending
- [ ] `/src/api/auth/send-magic-link.ts` - Request endpoint
- [ ] `/src/api/auth/verify-magic-link.ts` - Verification endpoint
- [ ] `/src/components/auth/LoginForm.tsx` - Login UI
- [ ] `/src/components/auth/MagicLinkVerification.tsx` - Verification UI
- [ ] `/src/styles/auth.css` - Auth page styles
- [ ] `/database/migrations/001_magic_tokens_table.sql` - DB migration
- [ ] `/tests/auth/token-generator.test.ts` - Token generation tests
- [ ] `/tests/auth/full-flow.test.ts` - Integration tests
- [ ] `/docs/AUTH.md` - Authentication documentation
- [ ] `/emails/magic-link.html` - Email template

### To Be Modified
- [ ] `/src/pages/Login.tsx` - Add login form
- [ ] `/src/pages/Auth.tsx` - Add verification page
- [ ] `/src/router.tsx` - Add auth routes
- [ ] `/src/lib/api-client.ts` - Add auth headers

---

## Change Log

| Date | Author | Change |
|------|--------|--------|
| 2026-03-06 | AIOS Framework | Story 1.1 created - Magic Link Authentication |

---

## Acceptance Checklist

- [ ] All AC1-AC6 acceptance criteria verified
- [ ] Code review approved
- [ ] All tests passing
- [ ] Linting and type checking pass
- [ ] Documentation complete
- [ ] Security review completed
- [ ] Manual testing completed
- [ ] Accessibility verified
- [ ] Ready for deployment
